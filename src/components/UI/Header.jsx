import styled from 'styled-components';

const Header = () => {
  return <HeaderWrap>PlanIt</HeaderWrap>;
};

const HeaderWrap = styled.div`
  padding: 1.4em 0;
  font-size: 1.3em;
  font-weight: bold;
  color: #fff;
`;

export default Header;
